[project]
name = "demandcast"
version = "0.1.0"
description = "Retail demand forecasting engine: rolling-origin backtesting, probabilistic LightGBM forecasts, planning dashboard."
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "pandas>=2.2,<3",
    "numpy>=1.26,<3",
    "pyarrow>=16",
    "statsforecast>=1.7",
    "mlforecast>=0.13",
    "utilsforecast>=0.2",
    "lightgbm>=4.3",
    "scikit-learn>=1.5",
    "pandera>=0.20",
    "mlflow>=2.14",
    "plotly>=5.22",
    "pydantic-settings>=2.3",
    "pyyaml>=6.0",
    "httpx>=0.27",
    "typer>=0.12",
    "rich>=13",
    "streamlit>=1.36",
]

[dependency-groups]
dev = [
    "pytest>=8.2",
    "pytest-cov>=5.0",
    "ruff>=0.5",
    "pre-commit>=3.7",
]

[tool.uv]
# See fraud-sentinel: broken ujson wheel on Windows boxes without msvcp140.dll.
override-dependencies = ["ujson; sys_platform == 'linux'"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/demandcast"]

[tool.ruff]
line-length = 100
target-version = "py312"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "SIM", "RUF"]
ignore = ["E501", "RUF001", "RUF002", "RUF003"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q --strict-markers"
markers = [
    "slow: tests that train on larger data or hit the network",
]

[tool.coverage.run]
source = ["src/demandcast"]
